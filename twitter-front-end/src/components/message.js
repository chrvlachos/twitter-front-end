import React from 'react'
import PropTypes from 'prop-types'

const message = props => {
    return (
        <div>
            
        </div>
    )
}

message.propTypes = {

}

export default message
